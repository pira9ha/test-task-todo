import {css} from "styled-components";

export const Label = css`
  position: relative;
  cursor: pointer;
  margin: 0 15px 0 0;
  width: 30px;
  height: 30px;
`;

export const ButtonNormalize = css`
  font-size: inherit;
  font-family: inherit;
  height: fit-content;
  border: 0;
  margin: 0;
  padding: 0;
  display: flex;
  align-items: center;
  background-color: transparent;
  cursor: pointer;
`;

export const HoverContainer = css`
  background-color: rgba(220, 190, 189, 0.18);
  border-radius: 5px;
`;
